# Git Hooks

This project uses [Lefthook](https://github.com/evilmartians/lefthook) for Git hooks.

## Pre-commit Behavior

Before each commit, staged files are checked with Biome to enforce formatting and lint quality.

## Setup

Hooks are installed automatically during:

```bash
npm install
```
